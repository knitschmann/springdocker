spring.application.name: Public Transport

server:
  port: 8080

management:
  security:
    enabled: false

spree:
  externalDataUrl: https://www.bvg.de/modules/TrafficNote/exporter.php
  connectionTimeout: 2000
  socketTimeout: 5000
cache:
  maxSize: 70 #amount of transport types * amount of publications
